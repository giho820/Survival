<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.survivalsos.goldentime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.IntroAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.GuideAddedMainAct"
            android:label="@string/title_activity_guide"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".activity.ArticleListAct" />
        <activity android:name=".activity.ArticleDetailAct" />
        <activity
            android:name=".activity.CheckListAct"
            android:label="@string/title_activity_check_list"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.SearchAct" />
        <activity android:name=".activity.DrawerNotificationAct" />
        <activity android:name=".activity.BookmarkAct" />
        <activity android:name=".activity.ImportToCheckListAct" />
        <activity
            android:name=".activity.AddCheckListAct"
            android:theme="@style/Theme.Transparent" />
        <activity android:name=".activity.ArticleDetailCheckListImportAct" />

    </application>

</manifest>